package com.cinerama.backend.exception;

public enum ErrorCode {
    USER_NOT_FOUND,
    INVALID_ARGUMENT,
    CONFLICT,
    VALIDATION_ERROR,
    CONSTRAINT_VIOLATION,
    ACCESS_DENIED,
    INTERNAL_ERROR
}